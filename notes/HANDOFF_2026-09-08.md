# HFG SESSION HANDOFF — September 8, 2026

**Read this entire file before touching any repository file, script, or
manuscript. Do not rebuild context from manuscripts alone — several
contain claims that have been retracted but not yet edited out.**

---

## 0. HARD RULES — READ FIRST

These exist because each was violated at least once and cost real time.

1. **Memory entries 11–22 in `/areas/hyperbolic-flavor-geometry.md` are
   authoritative history.** Multiple "discoveries" this month were
   re-derivations of results already documented there in May 2026.
   Check them before investigating anything that feels new.

2. **Never adjust a convention until a target number appears.** If a
   script does not reproduce a claimed value, find the script that
   originally produced it via `git log --all -S "<value>"`. Do not
   reverse-engineer the convention.

3. **Never overwrite a reproduction path.** If a script reproduces an
   older canonical value, add the new construction as a separate named
   function. Submitted papers cite specific scripts.

4. **PMNS holonomy requires WSL + conda sage.** Use
   `np.array(rho(word), dtype=complex)`. Windows and explicit
   `complex(mat[i][j])` give different, wrong holonomy. CKM reproduces
   on both platforms; PMNS does not.

5. **Always pass `fundamental_group_args=[True,False,True,False]`.**
   SnapPy's default returns a different generating set. This caused the
   CP phase discrepancy (195.91° vs 123.96°) and one ITF certificate
   failure before it was caught.

6. **Verify before recording.** Two errors this month were caught only
   because a value was recomputed rather than trusted: the $\lambda$
   longitude transcription, and a hand-rolled Smith normal form that
   disagreed with Sage.

7. **A `[WARN]` in a verification log is a stop condition.** In June
   2026 `verify_corrected.py` flagged the PMNS Borel fitness as
   non-reproducing. The flag was not acted on and the number stayed in
   the manuscript for three months.

---

## 1. PROVED — EXACT, CERTIFIED, COMMITTED

All certificates are exact symbolic computation or interval-certified
as noted. None depends on `algdep`, PSLQ, or numerical polynomial
recognition in a load-bearing step.

### 1.1 CKM invariant trace field

$$k_{\rm inv}(m006(-5,2)) \cong K_{10} = \mathbb{Q}[X]/(q_{10}(X))$$

$q_{10}(X) = X^{10} - 7X^8 + 4X^7 + 17X^6 - 14X^5 - 18X^4 + 14X^3 + 8X^2 - 3X - 1$

- Degree 10, disc $= -271{,}488{,}204{,}251$, signature $(8,1)$,
  Galois group $S_{10}$
- Six-step certificate chain, no algdep anywhere

| Step | Content | Commit |
|---|---|---|
| 1 | Presentation elimination recovers $m_\mu(T)$ without inserting it | fef0c91 |
| 2 | Dehn relator certificate: $qs = sq = 1$, 6-step reduction, no Knuth–Bendix | — |
| 3 | Geometry bridge: $\chi_{\rm geom} \in \operatorname{Spec} A_0(\mathbb{C})$ | 98761e0, 1f2d243 |
| 4 | Factor selection: $\mu_x = q_{10}^3 f_{200,1} f_{200,2}$, box selects $q_{10}$ | fef0c91 |
| 5 | Q-001: $\dim_{\mathbb{Q}} B = 20$, $u^2 = 0$, $\operatorname{rank} M_u = 10$, $B_{\rm red} \cong K_{10}$ | e419cfa |
| 6 | Squared-trace reverse inclusion | — |

Bridge log SHA256: `C1CB8DDB20D183A365F535ACF80C2B913CD2FE8830027350BF2ECE58AB24A5D2`

Scripts: `reproduce/ckm_presentation_elimination.sage`,
`ckm_presentation_geometry_bridge.sage`,
`ckm_factor_selection_audit.sage`, `ckm_dehn_relator_certificate.log`

### 1.2 PMNS invariant trace field

$$k_{\rm inv}(m003(-2,3)) \cong K_{283} = \mathbb{Q}[X]/(X^4+X^3-1)$$

- Degree 4, disc $= -283$, signature $(2,1)$
- All four gates; $A_{\rm fill}$ is already a field (radical, prime,
  $\dim_{\mathbb{Q}} = 4$) — no nilpotent reduction needed, unlike CKM
- Commits: `dbcc03d` (main), `4d6ea1d` (corpus)
- Script: `reproduce/pmns_itf_certificate.sage`

**Generator note:** the eliminant is the minimal polynomial of
$\operatorname{tr}\rho(\mu)$, giving $E(T) = T^4+3T^3-5T^2-11T+13$.
The historically quoted $x^4+x^3-1$ is the minimal polynomial of
$\operatorname{tr}\rho(a)$. Same field, different generator. This was
never reconciled in circulating summaries and caused confusion.

### 1.3 m009 normalizer

$$N_K(R)/\Gamma_{009} \cong C_2 \times C_2, \qquad [N_K(R):\Gamma_{009}] = 4$$

- $R_{\bar{\mathfrak p}}$ is a **Bass order** — 67-subspace exhaustive
  enumeration, doubly certified
- $N(R_{\bar{\mathfrak p}}) = N(E_{\bar{\mathfrak p}})$, finite residue
  image $\cong D_8$
- $\Gamma_{009} \trianglelefteq N_K(R)$ — proved, not assumed
- Complete overorder poset: $R \subset E \subset M_0, M_1$
- Key mechanism: $a = \sqrt{D_a} \cdot A_0$ with $A_0 \in M_2(K)$, so
  $\sqrt{D_a}$ cancels in commutators $JaJ^{-1}a^{-1}$, making all
  normality checks $K$-rational
- $\Gamma_{009}^+ = \pi_1(s780) = \pi_1(L6a1)$, $H_1 = \mathbb{Z}^2$,
  two cusps
- Commits: `e940e4d` (main), `5f2d415` (corpus)
- Script SHA256: `B659D8FCB5158F23AD0FC076193ABA213DE4E77ADDE4B13DB4A39E8871EF9679`

### 1.4 m003 homology surgery law

$$|H_1(m003(p,q);\mathbb{Z})| = 5\,|2p+q|$$

Proved from the presentation. Relator $r = \texttt{abAAbabbb}$ gives
torsion order 5; peripheral curves give the coefficient 2. Therefore
$H_1 \cong \mathbb{Z}/5 \iff |2p+q| = 1$, giving the ray
$(-2,3), (-3,5), (-4,7), \ldots$

**ℤ/5 is not a fitted parameter.** It is forced by the Eisenstein cusp
geometry.

### 1.5 m003 character variety — geometric component

$$I(X_0) = \langle xz+1,\ x^2+y-1 \rangle, \qquad X_0 \cong \mathbb{G}_m$$

Coordinate ring $\mathbb{Q}[X_0] \cong \mathbb{Q}[x,x^{-1}]$, with
parametrization $\Phi:(x,y,z) \mapsto (t,\ 1-t^2,\ -t^{-1})$ and
$\ker\Phi = I(X_0)$.

Three universal squared-trace identities on $X_0$:
$$\sigma_A = \sigma_{ABBB}, \qquad \sigma_{AB} = \sigma_{ABB},
\qquad \sigma_{AAb} = \sigma_{AABB}$$

**Stage 4 mechanism** — with $g_1 = yz - x - z$:
$$D_2 = -g_1, \qquad D_1 = (y+1)D_2, \qquad
D_3 = (y+1)\big[(x^2+y-1) - (xz+1)\big]$$

The identities are explicit combinations of the two generators of
$I(X_0)$. Ambient decomposition:
$$\sqrt{J_D} = I(X_0) \cap I(L) \cap I(M)$$
with $L = V(y+1,\ x+2z)$, $M = V(y-2,\ x-z)$. Restricted to the Riley
variety, $V(I_{\rm Riley}) \cap V(J_D) = X_0$ **set-theoretically**
(not scheme-theoretically on the raw relator ideal — do not upgrade).

Commit: `70a3b81` (main). Corpus `27fc6e5` — **verify pushed before
citing.**

### 1.6 The (−2,3) characterization

$$X(m003)^{\rm irr} \cap V\!\left(\operatorname{tr}^2\rho(B) -
\operatorname{tr}^2\rho(Abb)\right) = X_{-2,3}^{\rm irr}$$

- $B = b^{-1}$, $Abb = a^{-1}b^2$
- $\operatorname{tr}\rho(B) = y$, $\operatorname{tr}\rho(Abb) = xy^2 - yz - x$
- Signed branch $C_+$: gives $X_{-2,3}$ plus a reducible component
  $V(y+z+1,\ x+z+1,\ z^2+z-1)$
- Negative branch $C_-$: exactly $\varepsilon(X_{-2,3})$ where
  $\varepsilon$ is the sign character inducing $(x,y,z) \mapsto (-x,y,-z)$
- $\varepsilon$ preserves the cusped relator but $\varepsilon(s) = -1$
  on the slope word, so the minus branch has filling word $\mapsto -I$.
  **It is not a second $SL_2$ representation of $\pi_1(M)$** — it is the
  sign-twisted lift of the same $PSL_2$ representation.
- Commits: `013f63a`, `4ff56a8`, `10d7734`

### 1.7 Group-theoretic mechanism

$$BaBA \cdot B \cdot (BaBA)^{-1} = Abb^{-1} \quad \text{in } \pi_1(m003(-2,3))$$

Proved exactly via Fricke chart algebra after numeric search located
$g = BaBA$. Holds for every $SL_2(\mathbb{C})$ representation of the
filled group, not just the geometric one.

### 1.8 Stage 3 — conjugacy classification

| pair type | tr equal | homology inverse | conj-to-inverse | under ε-twist |
|---|---|---|---|---|
| 3 universal $X_0$ pairs | ✓ | ✓ | ✗ (exact) | $T_+$ preserved |
| B/Abb | ✓ | ✓ | ✓ (exact) | $T_+ \to T_-$ |

- Stage 3A: conjugate-to-inverse in the **cusped** group — exactly ruled out
- Stage 3.2: exact non-conjugacy via order-5616 finite quotient, doubly verified
- Stage 3.3: broader trace/homology enrichment **collapses under
  deduplication** — 40 occurrences = 3×13 + 1, i.e. four distinct facts
- **Stopping rule adopted:** no automatic Stage 3.4

### 1.9 Target-free atlas dichotomy

13 fillings, 99 canonical word classes, 1768 word-pairs. Protocol
frozen and hashed before results were examined.

- All 7 fillings with $H_1 \cong \mathbb{Z}/5$: **every** collision is
  an inverse-homology pair (22/22, zero exceptions)
- All 6 fillings with other $H_1$: **every** collision is neither same
  nor inverse (18/18, zero exceptions)

Categorical dichotomy tied to the ℤ/5 stratum specifically — not
universal, not (−2,3)-specific.

Bonus consistency: (−2,3) has 4 collisions, the other six ℤ/5 fillings
have exactly 3 each — independently reproducing the
three-universal-plus-one-exceptional structure.

Script: `reproduce/m003_structure_atlas.sage`

### 1.10 Census scan

Complete: 212,641/212,641 manifolds.
- 1,839 resolved (0.865%), 165 distinct fields
- Family rates: m 30.6%, s 9.9%, v 3.8%, t 2.4%, o 0.62% (~50× spread —
  the resolved set is **not** a random sample)
- 4,296,004 pairwise-disjoint 4-tuples among the 165 fields
- 3,648 match type $\{C_2, S_4, C_2, S_3\}$
- **Exactly 1** uses prime set $\{3,7,59,283\}$ — the HFG tuple
- Frozen: `census_final_COMPLETE_aug27.json`, hash recorded

### 1.11 Galois product theorem (August 2026, not re-verified this month)

$$\operatorname{Gal}(L/\mathbb{Q}) \cong C_2 \times S_4 \times C_2 \times S_3,
\quad |{\cdot}| = 576$$

Proved by sequential degree multiplication; independent ramification
proof via disjoint prime supports $\{3\},\{283\},\{7\},\{59\}$.

**Note:** the symbol $L$ here is the four-field compositum. It is *not*
the m009 quartic field, and *not* $q_{10}$'s splitting field ($S_{10}$,
order 3,628,800). These got conflated once.

### 1.12 Peripheral character

$$Q_\partial(p,q) = 2p + q$$

The **only** integral character on $H_1(\partial m003;\mathbb{Z}) \cong
\mathbb{Z}^2$ up to scale, since $\operatorname{Hom}(\mathbb{Z}/5,
\mathbb{Z}) = 0$ kills the torsion generator.

$Q_\partial(-2,3) = -1$ (primitive); $Q_\partial(-5,2) = -8$.

---

## 2. RETRACTED — DO NOT RECONSTRUCT

Each of these appears in at least one manuscript still on disk.
**Remove before any submission.**

| Claim | Status | Reason |
|---|---|---|
| PMNS fit $p < 10^{-4}$ | **RETRACTED** | Corrected null: $k=6625/50000$, $\hat p = 0.1325$ |
| PMNS fitness statistically exceptional | **RETRACTED** | 3-param family, full rank 3, cond ≈ 4.1 — dimension counting explains it |
| $m003(-2,3)$ is global min-volume closed hyperbolic 3-manifold | **FALSE** | Weeks manifold, vol 0.942707, $H_1 = \mathbb{Z}/5 \oplus \mathbb{Z}/5$. Correct form: **min volume within the ℤ/5 stratum** |
| $k_{\rm inv}(m003(-2,3)) = \mathbb{Q}(\sqrt{-3})$ | **FALSE** | That is the **cusped** field. Closed filling is $K_{283}$ |
| Lucas-purity selects the manifold | **WEAKENED/RETRACTED** | Already weakened in the record before this month |
| $\sigma_{\rm opt} = (3/2)\log\varphi$ selects the manifold | **REFUTED** | ~213× off the CKM optimum |
| 122 trace classes / 12,288 freely reduced words | **WRONG** | Correct: **1,456** words ($4\sum_{j=0}^{5}3^j$), **66** classes numerically |
| $p_2 = 31$ unique among all Farey tower primes | **OVERCLAIM** | Proof covers only $p \le 281$. Scope to the tested set |
| Signature $(8,1)$ causes CP suppression | **UNSUPPORTED** | $J = 0$ follows from real kernel + real QR alone |
| Q-001 gives $z = x$ on the filling ideal | **FALSE** | Correct: $u = z-x \ne 0$, $u^2 = 0$, $\operatorname{Nil}(B) = (u)$, $B_{\rm red} \cong K_{10}$ |
| Symmetric floor $\ge 0.300$ | **NOT A THEOREM** | Finite random scan; actual value 0.1505 |
| $\mathbb{Z}/5 \to \mathbb{Z}$, $1 \mapsto -3$ charge map | **DOES NOT EXIST** | $\operatorname{Hom}(\mathbb{Z}/5,\mathbb{Z}) = 0$ |
| Peripheral mod-5 descent test | **VACUOUS** | $\mu, \lambda$ both die in $H_1$ of any filling — $0 \equiv 0$ |
| Published 4:2:1 torsion hierarchy | **UNSUPPORTED** | Class map never descended to $H_1$ |

---

## 3. UNRESOLVED

### 3.1 CP phase basis dependence

$\delta = \pi + \varphi(\texttt{aaB}) + \varphi(\texttt{baa})$ gives
**195.91°** under SnapPy's default basis and **123.96°** under the
verified cusped basis. 72° apart, same manifold, same nominal words.

The words are not well-defined without specifying the generating set,
and the manuscript never says which one. Three of four invariance
checks pass (conjugation, word-inverse, central sign twist). The
fourth fails.

**Not refuted — not well-posed.** No value should be asserted.

### 3.2 Original constrained Borel construction

$$\ell_{ij} = \lambda\, s_{ij}\, (\hat n_i \cdot \hat n_j)$$

One scale $\lambda$ (grid $[0.1,5.0]$, 50 values), 3 signs, ratios fixed
by geometry. **Genuinely manifold-dependent** — a completely different
null question from the 3-parameter version.

March 2026 reported words $\{\texttt{aa}, \texttt{ab}, \texttt{aB}\}$,
dot products $(0.443, -0.530, 0.432)$, fitness $0.01897$.

**Currently unreproducible.** All 16 `fundamental_group_args`
combinations tried; closest is $(0.604, 0.379, -0.320)$. Also: the
March paper's own text says $0.018968$ was the *free 6-parameter
minimum* found at exactly $(0.443,-0.530,0.432)$ — so the reasoning may
have run backwards, with words sought to explain a free optimum.

Status: **historically documented, numerically unreproduced.** An
archival problem, not a statistical one. Recovery needs an old
environment lockfile or a raw pre-conversion matrix dump.

### 3.3 $q_{10}$ multiplicity 3

$\mu_x = q_{10}^3 f_{200,1} f_{200,2}$. The exponent 3 was set aside as
unnecessary for the ITF theorem and never explained. Q-001 found
square-zero structure at depth 2 on the slice; depth 3 in the ambient
algebra suggests more nilpotent structure than the single $u = z-x$.

### 3.4 Why those three identities

Now largely answered by Stage 4 ($D_i \in I(X_0)$ explicitly). What
remains: whether a common syzygy of the trace-difference ideal mod
$I(X_0)$ gives a uniform reason. Open only if the theorem spine needs it.

---

## 4. SUBMISSION STATE

### 4.1 Active — do not touch

| ID | Paper | Status |
|---|---|---|
| PLB-D-26-01006 | Chirality | Under review since Apr 20 2026 |
| JKTR-S-26-00044 | Alexander polynomial | With journal |
| JTop p_id=56435 | Torsion | With Alan Reid |

### 4.2 DO NOT RESUBMIT — exhausted

PLB (6 rejections), NPB (7), PRD (7), RINP (5), AoP (2, Aug 25 2026),
AGT (Aug 24 2026, Purcell), LMP, EPJC, JNT, Math Z, AGAG

**Exception:** the CKM paper is now a materially different manuscript
(arithmetic-first, exact ITF theorem). The seven prior NPB rejections
were of a different claim structure. A **fresh** NPB submission — not
an Elsevier transfer — is defensible. Transfer carries the AoP history;
fresh submission allows a cover letter explaining what changed.

### 4.3 Structurally blocked

| Venue | Barrier | Confirmed |
|---|---|---|
| JHEP | arXiv ID required **at submission** (SCOAP3) | Portal, directly |
| SciPost | Institutional email; "only professional academics" | Registration, directly |

Stop proposing these.

### 4.4 Live

- SSRN 7341038 — Galois product theorem, DOI 10.2139/ssrn.7341038
- SSRN 7277458 — dual surgery

### 4.5 arXiv endorsement — the root bottleneck

20+ attempts including follow-ups, across hep-ph, hep-th, math-ph, math.GT.

| Contact | Sent | Response |
|---|---|---|
| Genevieve Walsh (Tufts) | Aug 18, follow-up Sep 1 2026 | None |
| Nathan Dunfield (nmd@illinois.edu) | Apr 19, Apr 28 2026 | None |
| Alan Reid (alan.reid@rice.edu) | Apr 19, Apr 28 2026 | None |
| LMP editor (Ad Hoc via EM) | Apr 2026 | None |

**Identified, drafted, no confirmed send:**
- Marc Culler — culler@math.uic.edu (SnapPy lead dev, UIC emeritus)
- Ian Agol — agol@math.berkeley.edu (Fields 2016, Berkeley)

**Identified, never drafted — cleanest contact:**
- Matthias Goerner — goerner@pixar.com (SnapPy co-dev, actively
  publishing, "mathematician by night")

Code `SIQW6F` (May 2026, math.GT) is **almost certainly expired** —
30-day window. Generate a fresh code before contacting anyone.

**Recommended pitch:** lead with the m009 Bass order theorem. It is a
narrow, checkable claim about a specific census manifold that a
SnapPy-ecosystem person can evaluate in 15 minutes. Do not ask them to
vouch for HFG physics.

---

## 5. MANUSCRIPTS

### 5.1 New canonical PMNS/m003 paper

`papers/gentry-m003-arithmetic-v4.tex` — commit `aa2383a`

Title: *Arithmetic and Character-Variety Structure of the Closed
Hyperbolic 3-Manifold m003(−2,3)*

Structure: surgery law → ITF theorem → presentation/geometry bridge →
character variety → Borel construction (downstream) → null calibration
(negative result) → open questions.

§1.1 lists explicit non-claims so no referee reconstructs them.

**Verified:** relator `abAAbabbb`, meridian `ABABB`,
longitude `ABAbab` (corrected from `AAbA` — that was a transcription
error; the proof script always used the right one). Filling relation
$\mu^{-2}\lambda^3 = +I$ confirmed at 300 bits, off-diagonals ~$10^{-89}$.

**Pending:** fold in Stage 4 as a formal theorem replacing the current
"verified by reduction mod $P_0$" phrasing.

`gentry-pmns-plb.tex` — **retained, not deleted.** Cited by submitted
work.

### 5.2 CKM manuscript — three edits before submission

`CKM_THEOREM_AUDIT.md` (repo root, commit `3238a9a`) has the full
42-item register.

1. Word count **12,288 → 1,456**; classes **122 → 66**
2. **Remove** the CP-suppression section — it asserts
   $k_{\rm inv}(M_{\rm PMNS}) = \mathbb{Q}(\sqrt{-3})$, which is wrong
   (degree 4, disc −283)
3. $p_2 = 31$: scope to $\{11,41,61,101,151,211,281\}$

Then: replace the old ITF proof (SnapPy `find_field` recognition) with
the exact six-step chain from §1.1 above.

### 5.3 Dual surgery paper

Revised: $D_6 \cong S_3 \times C_2$ fixed (Remark 10 — the conclusion
**flips** to supporting Pati–Salam), discriminant proofs added for both
$C_2 \times S_4$ and $C_2 \times S_3$, abstract promotes the paired
structure. AGT rejected Aug 24. Needs a venue.

---

## 6. TWO CKM CONSTRUCTIONS — BOTH VALID

They differ in permutation convention **and** word triple. Not
competing values for the same quantity.

| | v1 (`ckm_gaussian_v1`) | v2 (`ckm_gaussian`) |
|---|---|---|
| $\mathcal{F}$ | 0.016482 | 0.002728 |
| $\sigma$ | 0.49 | 0.47 |
| words | aaB/AbA/AAb | aaab/aabb/bAbAB |
| permutation | row+column joint | column only |
| origin | memory entry 11, May 11 2026 | commit `b169562`, Jul 12 2026 |
| script | — | `verify_new_global_best.py` |
| cited by | `correction_letter_RINP_00327_CKM.txt` | current CKM manuscript |
| claim scope | best among ℤ/5 + Lucas-pure | global best, census rank 1 |

Both restored side by side in `hfg_reproduce.py`. Both reproduce
exactly. **Do not collapse them.**

Continuous refinement of v2: $\sigma^\star = 0.469328362$,
$\mathcal{F}^\star = 0.00233065$ (`sigma_continuous_refine.py`).

---

## 7. NEXT TARGETS — RANKED

**1. m006 character variety.** Direct analogue of §1.5. The pipeline
exists. Is $X_0(m006)$ rational? What is $I(X_0)$? If it is also
$\mathbb{G}_m$ that is a structural fact about both flavor manifolds;
if higher genus, the asymmetry itself is informative. Estimated: one day.

**2. Laurent word invariants.** Since $X_0(m003) \cong \mathbb{G}_m$,
every word $w$ gives an element of $\mathbb{Z}[t,t^{-1}]$. Degree,
Newton polygon, coefficient pattern are **canonical** word invariants in
a way word length never was. This is the first plausible route to a
*derived* word-selection rule rather than a fitted one — the gap every
version of the phenomenology has fallen into. Preregisterable.

**3. $q_{10}$ multiplicity-3 decomposition.** See §3.3.

**4. Bass classification for m010 and m006.** m010 left open (level
(2,2), non-integer index, needs Jun–Kim dyadic framework). m006's
holonomy order never classified. Pattern or discriminator either way.

**5. $Q_\partial$ asymmetry.** PMNS slope gives the primitive value
$\pm 1$; CKM slope gives $-8$. Understand the asymmetry before
interpreting it.

---

## 8. ENVIRONMENT

- WSL + conda `sage` environment. Sage 10.9, SnapPy 3.3.2, Python 3.13.14
- Main repo: `C:\dev\hyperbolic-flavor-geometry\`
- Corpus: `C:\dev\HFG-CORPUS\`
- Scan repo: `C:\dev\hyperbolic-flavor-scan\` (separate history; local
  and remote have **diverged** — pre-existing, left untouched)
- Gap report: `MASTER_GAP_REPORT.md`, mirrored in both main repos
- `.log` files are gitignored — use `git add -f` to commit them
- Pushes from WSL have hung; PowerShell git works

**Division of labor:**
- Cowork — executor, file access, computation, repo writes
- Codex — read-only auditor, verification, no repo writes
- Web chat — coordination, memory, task routing

---

## 9. EVIDENCE LABELS

Tag every recorded result:

- `[Proved]` — complete proof, exact or interval-certified
- `[Computed]` — numerical verification, reproducible, not a theorem
- `[Statistical]` — Monte Carlo, report $k/N$ with binomial CI, never
  a bare $p$-value
- `[Conjecture]` — unproved

Flag any upgrade in evidence level explicitly. Never silently promote
`[Computed]` to `[Proved]`.

---

*End of handoff.*
